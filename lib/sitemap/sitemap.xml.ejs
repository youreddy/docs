<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <% urls.forEach(function (url) { %>
  <% if (url.indexOf('/') === 0) { %>
    <url>
        <loc>https://docs.auth0.com<%= url %></loc>
      <priority><%= url === '/' ? '1.0' : '0.9' %></priority>
      <changefreq>weekly</changefreq>
    </url>
  <% } %>
  <% }); %>
</urlset>
